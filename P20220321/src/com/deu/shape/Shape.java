package com.deu.shape;

public class Shape {

	// int값 x, y 2개의 필드 선언
	// GetArea - 도형의 넓이를 구하는 메소드 설정
	// Shape클래스를 상속받는 3개의 클래스(사각형, 원, 삼각형)를 생성, 넓이를 구하도록 지정
	// {x*y, r*r*3.14, (x*y)/2 } 자식 클래스에서 재정의
	
	private int x, y;
	
	public double getArea() {
		return 0;
	}
}
